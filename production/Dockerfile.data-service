FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json
COPY services/data-service/package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY services/data-service/ ./

# 编译TypeScript
RUN npm run build

# 暴露端口
EXPOSE 3000

# 启动服务
CMD ["npm", "start"] 